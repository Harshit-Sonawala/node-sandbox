import os from 'os';

// userInfo()
console.log(os.userInfo());

//totalmem() - in bytes
console.log(os.totalmem());

// freemem()
console.log(os.freemem());

// cpus() - object for every core of the system processor
console.log(os.cpus());
